<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.situ.mall.dao.CategoryDao">
	<resultMap type="Category" id="categoryMap">
		<id property="id" column="id"/>
		<result property="parentId" column="parent_id"/>
		<result property="name" column="name" />
		<result property="status" column="status" />
		<result property="sortOrder" column="sort_order" />
		<result property="createTime" column="create_time" />
		<result property="updateTime" column="update_time" />
	</resultMap>
	
	<sql id="Base_Column_List">
		id,parent_id,name,status,sort_order,create_time,update_time
	</sql>
	<!-- 查找所有 -->
	<select id="findAll" resultMap="categoryMap">
		select <include refid="Base_Column_List"></include> from category
	</select>
	
	
	<select id="findParentCategory" resultType="Category" >
		select id,name from category where parent_id=0;
	</select>
	
	<select id="findPCategory" resultMap="categoryMap" >
		select id,parent_id,name from category where parent_id=#{id};
	</select>
	
</mapper>
